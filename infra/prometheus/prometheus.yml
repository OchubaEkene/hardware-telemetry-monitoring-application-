global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: hardware_agent
    static_configs:
      - targets: ["192.168.0.118:9100"]
        labels:
          instance: home-pc
